---
description: Load current project state and report where things stand
---
Read `EFIMERAMENTE_STATE.md` (sections: Project Overview, Key Files, Pending / Backlog,
Working Protocol). Do NOT read `CHANGELOG.md` unless I ask about past work.

Then run `git status --short` and `git log -3 --oneline`.

Report, in under 15 lines:
1. What is uncommitted or unpushed right now
2. The top 3 pending items from the backlog, with your read on which is highest leverage
3. Anything in the state file that looks stale or contradicted by the repo

Do not start work. This is a status read only.
